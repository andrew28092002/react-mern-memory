import { SxProps } from "@mui/material";
import React from "react";

export const commentsOuterContainer: React.CSSProperties = {
    display: "flex",
    justifyContent: "space-between",
  };
  export const commentsInnerContainer: React.CSSProperties = {
    height: "200px",
    overflowY: "auto",
    marginRight: "30px",
  };